<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manu</title>
    <!-- Link to Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap" rel="stylesheet">
    <!-- Link to custom login.css -->
    <link rel="stylesheet" href="/main.css">
    <style>
        .fancy-text {
            font-family: 'Great Vibes', cursive;
        }
    </style>
</head>

<body>
    <img src="/assets/bg.jpg" alt="Image" class="img-fluid" id="image-bg">
         <div class="card " id="card">
            <div class="container mt-5">
                <h1 class="text-center fancy-text">My Vinyl</h1>
            </div>
        </div>
            <div class="card-menu" id="card-menu">
                <img src="/assets/CDisc.webp" alt="Image" class="img-fluid" id="image2">
                <br>
                <br>
                <br>
                <br>
                
            <svg width="400" height="120">
                <!-- ปรับเส้นโค้งใหม่ -->
                <path id="curve" d="M0,100 Q200,0 400,100" fill="transparent" />
            
                <text width="400">
                    <textPath xlink:href="#curve" startOffset="50%" text-anchor="middle" font-size="32" fill="#242424"
                        font-family="Great Vibes, cursive">
                        My Vinyl
                    </textPath>
                </text>
            </svg>

                <input type="file" id="upload" accept="image/*">
                <br><br>
                <img id="preview" src="" alt="รูปตัวอย่าง" style="max-width: 400px; display: none;">
            </div>
        

    <img src="/assets/CDisc.webp" alt="Image" class="img-fluid" id="image">
    <img src="/assets/envelope2.png" alt="Image" class="img-fluid" id="hand">
    <!-- Link to Bootstrap 5 JavaScript (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
    const img = document.getElementById('image');
     const card = document.getElementById('card');
     const hand = document.getElementById('hand');
     const card2 = document.getElementById('card-menu');
        window.addEventListener('DOMContentLoaded', function () {
            setTimeout(() => {
                img.classList.add('animate-to-left');
            }, 500); // รอ 100ms เพื่อให้ browser apply class ก่อน
                
            setTimeout(() => {
                card.classList.add('animate-to-left');
            }, 1000); 
            
             setTimeout(() => {
                hand.classList.add('animate-to-left');
            }, 1000);
            setTimeout(() => {
                hand.classList.add('animate-to-bottom');
            }, 2000);
            setTimeout(() => {
                card2.classList.add('show');
            }, 2000);
        });

            const uploadInput = document.getElementById('upload');
            const previewImg = document.getElementById('preview');

            uploadInput.addEventListener('change', function (event) {
                const file = event.target.files[0];

                if (file) {
                    const reader = new FileReader();

                    reader.onload = function (e) {
                        previewImg.src = e.target.result; // อ่านไฟล์มาเป็น base64 แล้วแปะ src
                        previewImg.style.display = 'block'; // โชว์รูป
                    };

                    reader.readAsDataURL(file); // อ่านไฟล์แบบ data URL
                }
            });
    </script>
</body>

</html>